import { Box, styled } from "@mui/material";

const FullFlexCenteredBox = styled(Box)({
  display: "flex",
  justifyContent: "center",
  alignItems: "center",
});

export default FullFlexCenteredBox;
